<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.eschoe.hexagonal.order.adapter.out.persistence.ShipmentMapper">

    <resultMap id="shipmentResult" type="io.github.eschoe.hexagonal.order.adapter.out.persistence.ShipmentEntity">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="address" column="address"/>
        <result property="status" column="status"/>
        <result property="dispatchedAt" column="dispatched_at"/>
        <result property="deliveredAt" column="delivered_at"/>
    </resultMap>

    <select id="findByOrderId" resultMap="shipmentResult">
        SELECT id, order_id, address, status, dispatched_at, delivered_at
        FROM shipments
        WHERE order_id = #{orderId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO shipments (order_id, address, status, dispatched_at, delivered_at)
        VALUES (#{orderId}, #{address}, #{status}, #{dispatchedAt}, #{deliveredAt})
    </insert>

    <update id="update">
        UPDATE shipments
        SET address = #{address},
            status = #{status},
            dispatched_at = #{dispatchedAt},
            delivered_at = #{deliveredAt}
        WHERE id = #{id}
    </update>

</mapper>
